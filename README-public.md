# Filona public deployment

1. Push this project to GitHub.
2. Create a free Render account.
3. Import the repository.
4. Use the provided Render config or deploy the server as a web service.
5. Set VITE_API_URL to your deployed backend URL.
