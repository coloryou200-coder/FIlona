services:
  - type: web
    name: filona-server
    env: node
    plan: free
    buildCommand: cd apps/desktop && npm install
    startCommand: cd apps/desktop && node server.mjs
    envVars:
      - key: NODE_ENV
        value: production
