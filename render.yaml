services:
  - type: web
    name: filona-server
    env: node
    plan: free
    root: apps/desktop
    buildCommand: npm install
    startCommand: node server/index.mjs
    envVars:
      - key: NODE_ENV
        value: production
